  <!DOCTYPE html>
<html>
<head>
  <title></title>
</head>
<body>

</body>
</html><nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <%- if user_signed_in? %>
        <%= link_to "JB", society_path, class: "navbar-brand" %>
      <% else %>
        <%= link_to "JB", root_path, class: "navbar-brand" %>
      <% end %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <%- if user_signed_in? %>
          <li><a href="#"><%= t('wiadomosci') %><span class="sr-only">(current)</span></a></li>
        <% else %>
          <li><%= link_to t(:register), new_user_registration_path %></li>
          <li><%= link_to t("Zaloguj się"), new_user_session_path %></li>
        <% end %>
      </ul>
    <%- if user_signed_in? %>
      <form class="navbar-form navbar-left" role="search">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <%= link_to t("szukaj"), "#", class: "btn btn-default" %>
      </form>

      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to current_user.email, user_path(id: current_user.id) %></li>

        <li><%= link_to image_tag('unitedKingsom'), locale: :en %></li>
        <li><%= link_to image_tag("poland"), locale: :pl %></li>

        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="glyphicon glyphicon-user" aria-hidden="true"></span> <%= t("moj_profil") %> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to t(:sakramenty), user_path(id: current_user) %></li>
            <li><%= link_to t(:edytuj_dane), edit_user_registration_path(id: current_user) %></li>
            <li><%= link_to t(:posty), new_user_post_path(user_id: current_user) %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to t(:signed_out), destroy_user_session_path, method: :delete, data: {confirm: "Czy na pewno chcesz się wylogować?" } %></li>
          </ul>
        </li>
      </ul>
      <% else %>
        <ul class="nav navbar-nav navbar-right">

        </ul>
      <% end %>



    </div>
  </div>
</nav>

