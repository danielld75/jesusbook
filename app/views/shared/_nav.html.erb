<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <%- if user_signed_in? %>
        <%= link_to "JB", user_path(id: current_user), class: "navbar-brand" %>
      <% else %>
        <%= link_to "JB", root_path, class: "navbar-brand" %>
      <% end %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <%- if user_signed_in? %>
          <li><a href="#">Wiadomości<span class="sr-only">(current)</span></a></li>
        <% else %>
          <li><%= link_to t(:register), new_user_registration_path %></li>
          <li><%= link_to t("Zaloguj się"), new_user_session_path %></li>
        <% end %>
      </ul>
    <%- if user_signed_in? %>
      <form class="navbar-form navbar-left" role="search">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <%= link_to "Szukaj", "#", class: "btn btn-default" %>
      </form>

      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to current_user.email, user_path(id: current_user.id) %></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="glyphicon glyphicon-user" aria-hidden="true"></span> Mój Profil <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Sakramenty", user_path(id: current_user) %></li>
            <li><%= link_to "Edytuj dane", edit_user_registration_path(id: current_user)%></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to I18n.t(:signed_out), destroy_user_session_path, method: :delete, data: {confirm: "Czy na pewno chcesz się wylogować?" } %></li>
          </ul>
        </li>
      </ul>
      <% else %>
        <ul class="nav navbar-nav navbar-right">

        </ul>
      <% end %>
    </div>
  </div>
</nav>
